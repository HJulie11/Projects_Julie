import mink
import numpy as np

""" I need to use mink ik solver to solve inverse kinematics for the hand (examples/hand_shadow.py of mink is a big reference)."""

class IKSolver:
    def __init__(self,)
        

    def configuration(self):
        return mink.Configuration(self.env.physics.model._model)
    
    def 